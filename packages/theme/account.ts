import { StyleSheet } from 'react-native'

import { xxl } from 'theme/include/spacing'
import { secondary } from 'theme/include/colors'

export default StyleSheet.create({
  tabs: {
    paddingTop: xxl
  },

  secondaryTabs: {
    backgroundColor: secondary
  }
})
