import { StyleSheet } from 'react-native'

import { xxl, xl, l, m, s, xs } from 'theme/include/spacing'
import { white } from 'theme/include/colors'

export default StyleSheet.create({
  main: {
    paddingTop: xl,
    backgroundColor: white
  }
} as any)
